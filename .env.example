# Called LA Home currently runs without required environment variables.
# Keep secrets out of NEXT_PUBLIC_* variables because those values are exposed to the browser.

# Reserved for future server-only integrations:
# API_SECRET_KEY=
